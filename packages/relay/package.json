{
  "name": "@aamp/relay",
  "version": "0.1.0",
  "description": "AAMP Relay Server — NATS JetStream message bus + HTTP federation gateway",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "node --watch --loader ts-node/esm src/index.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aamp/core": "workspace:*",
    "@aamp/identity": "workspace:*",
    "nats": "^2.28.2",
    "fastify": "^4.28.1",
    "@fastify/cors": "^9.0.1",
    "uuid": "^11.0.5"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "@types/node": "^22.10.7",
    "ts-node": "^10.9.2"
  }
}
